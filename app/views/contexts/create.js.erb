<% if @contexts.any? %>
  contexts_update(<%= @bbs_thread.id %>, "<%= escape_javascript(raw(render partial: 'shared/context', collection: @contexts)) %>");
<% end %>

contexts_no_update(<%= @bbs_thread.id %>, <%= @bbs_thread.contexts.count %>)
