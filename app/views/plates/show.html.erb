<%= render 'shared/plates' %>
<%= render 'shared/plate' %>
<%= render 'shared/bbs_threads', bbs_threads: @bbs_threads %>

<% @bbs_threads[0..9].each do |bbs_thread| %>
  <%= render 'shared/thread', bbs_thread: bbs_thread, contexts: bbs_thread.contexts.last(10) %>
<% end %>
